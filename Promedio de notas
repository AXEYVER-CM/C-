#include <iostream>
using namespace std;

int main() {
    float nota, suma = 0;
    for (int i = 0; i < 5; i++) {
        cout << "Ingrese nota " << i + 1 << ": ";
        cin >> nota;
        suma += nota;
    }
    cout << "Promedio: " << suma / 5 << endl;
    return 0;
}
